name: Trigger auto deployment for fiapcloudgamesgame

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/fiapcloudgamesgame-AutoDeployTrigger-70ea89fc-cda1-4b08-a7cc-e31aa2053806.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.FIAPCLOUDGAMESGAME_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.FIAPCLOUDGAMESGAME_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.FIAPCLOUDGAMESGAME_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: fiapcloudgameacr.azurecr.io
          registryUsername: ${{ secrets.FIAPCLOUDGAMESGAME_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.FIAPCLOUDGAMESGAME_REGISTRY_PASSWORD }}
          containerAppName: fiapcloudgamesgame
          resourceGroup: fiap
          imageToBuild: fiapcloudgameacr.azurecr.io/fiapcloudgamesgame:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


